# Supabase session pooler, port 5432. Use the restricted keep_app role.
SUPABASE_DB_URL=postgresql://keep_app.PROJECT_REF:URL_ENCODED_PASSWORD@POOLER_HOST:5432/postgres
# On Heroku set these; omit APP_ORIGIN for localhost development.
# NODE_ENV=production
# APP_ORIGIN=https://keep2-65fe1099bb65.herokuapp.com
PORT=3000
# Disposable, offline development only (never allowed in production):
# KEEP_MEMORY=1
